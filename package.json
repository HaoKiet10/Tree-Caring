{
  "name": "Tree-Caring",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -n \"WEB,SERVER\" -c \"cyan,green\" \"pnpm --filter=client run dev\" \"pnpm --filter=server run dev\"",
    "dev:client": "pnpm --filter=client run dev",
    "dev:server": "pnpm --filter=server run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
